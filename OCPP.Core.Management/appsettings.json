{
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  },

  "ConnectionStrings": {
    //"SQLite": "Filename=.\\..\\SQLite\\OCPP.Core.sqlite;"
    //"SqlServer": "Server=.;Database=OCPP.Core;Trusted_Connection=True;Encrypt=false;TrustServerCertificate=false"
  },

  // ServerApiUrl needs to be set to the base URL for the server process endpoint
  "ServerApiUrl": "http://localhost:8081/API",
  // API Key should be randomly generated and kept secure and matched to the server appsettings
  "ApiKey": null,
  // See development environment json for examples
  "Users": null,


  "AllowedHosts": "*",

  // Use "0.0.0.0" instead of "localhost" when the site should be reachable from other computers
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://localhost:8082"
      },
      "HttpsInlineCertFile": {
        "Url": "https://localhost:8092",
        "Certificate": {
          "Path": "localhost.pfx",
          "Password": "OCPP.Core"
        }
      }
    }
  }
}
